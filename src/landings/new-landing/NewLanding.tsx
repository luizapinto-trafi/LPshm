/**
 * New landing — blank canvas.
 * Empty starting point. Build the landing markup/styles here.
 */
import React from "react";

export const NewLanding = () => {
  return (
    <main
      style={{
        minHeight: "100vh",
        margin: 0,
        background: "#fff",
        color: "#292929",
        fontFamily: "AvenirNextLTPro, system-ui, sans-serif",
      }}
    >
      {/* Start building the new landing here. */}
    </main>
  );
};

export default NewLanding;
